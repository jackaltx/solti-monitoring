---
# Domain configuration
# Can be overridden by setting LAB_TLD environment variable
# Default: example.com for generic/portable repository usage

domain: "{{ lookup('env', 'LAB_TLD') | default('example.com', true) }}"
