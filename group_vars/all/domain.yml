---
# Domain configuration
# Can be overridden by setting LAB_DOMAIN environment variable
# Default: example.com for generic/portable repository usage

domain: "{{ lookup('env', 'LAB_DOMAIN') | default('example.com', true) }}"
